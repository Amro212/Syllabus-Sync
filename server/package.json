{
	"name": "server",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"security:config": "node scripts/check-security-config.mjs",
		"security:scan": "node scripts/security-release-gate.mjs",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.16.9",
		"typescript": "^5.5.2",
		"vitest": "^4.1.7",
		"wrangler": "^4.94.0"
	},
	"dependencies": {
		"ajv": "^8.20.0",
		"ajv-formats": "^3.0.1"
	}
}
